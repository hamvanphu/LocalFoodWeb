# [6] Risk List — Local Food

> Input: `EST-LF.md` (đã đóng Cổng hiểu bước [5], chốt Phương án A + checkpoint 2026-08-31).

| # | Rủi ro | Xác suất | Tác động | Mitigation | Owner |
|---|---|---|---|---|---|
| R1 | Vượt deadline (PERT ~50.7h > quỹ PM ~30-40h) | Cao | Cao | Checkpoint bắt buộc hết ngày 2026-08-31: nếu nền tảng (W1-1→W1-9) chưa xong, tự động chuyển Phương án B (5 tỉnh) | PM |
| R2 | **[Rủi ro AI-sinh]** Hallucination nội dung ẩm thực — AI viết sai lệch văn hoá/công thức | **Cao** (đo được 2026-09-06: **63% món không có nguồn Wikipedia**; 21/63 tỉnh có cả 3 món đều không) | Cao (uy tín nội dung) | `sourceRef` bắt buộc trong schema — **nhưng đã chứng minh là chưa đủ**: schema chỉ kiểm *có* URL, không kiểm URL *còn sống* (tìm thấy 3 link 404 nằm im, đã thay). Đã lập `SPOTCHECK-LF.md` với mẫu 5 tỉnh bốc ngẫu nhiên **từ nhóm 21 tỉnh rủi ro cao nhất** (seed cố định, tránh cherry-pick). **PM đã spot-check ở mức đọc lướt** 15/197 món (7,6%), 0 món đối chiếu chi tiết, không thấy sai rõ ràng — ghi đúng mức đó, không nâng thành "đã kiểm chứng". **PM chốt chuyển hướng mitigation sang phát hiện khi vận hành:** đã build US-15 (kênh *Báo nội dung sai* tách khỏi chấm sao). **Hạn chế còn lại, không giấu:** (a) site mới deploy, chưa có người dùng nên crowdsourcing chưa hoạt động lúc nộp bài; (b) PM chỉ thấy báo lỗi khi chủ động chạy SQL, chưa có thông báo tự động | PM (soi nội dung + xem báo lỗi định kỳ), AI (đã kiểm link, dựng phiếu, build kênh báo lỗi) |
| R3 | **[Rủi ro AI-sinh]** Rubber-stamping — PM duyệt bừa output AI, đặc biệt khi gấp deadline dễ có xu hướng bỏ qua Cổng hiểu | Trung bình (đã giảm nhiều qua phiên này) | Cao nếu tái diễn | Duy trì kỷ luật Cổng hiểu ở MỌI bước còn lại, kể cả khi gấp — đặc biệt bước [8] BUILD, nơi cám dỗ "để AI chạy nhanh cho kịp" cao nhất | PM |
| R4 | Ảnh Wikimedia thưa cho một số món đặc sản vùng miền (đã xác nhận: Bánh đa cua, Bún cá Nha Trang, Lẩu mắm, Bún cá Châu Đốc) | Cao (đã xảy ra thật) | Thấp (đã có fallback) | Placeholder gradient theo Design System (đã code ở `ImageWithFallback`) | AI (đã xử lý), PM (kiểm tra kết quả trông có ổn không) |
| R5 | Ảnh Wikimedia lỗi tải lúc runtime (đã xảy ra thật: 429 khi test dồn dập; **2026-09-06 tái xuất hiện trên production dưới dạng 502 qua `_next/image` khi cache Vercel còn lạnh**) | Cao (đã xảy ra 2 lần, ở 2 môi trường khác nhau) | Thấp (đã có fallback, layout không vỡ) | Fix GAP-01 (W1-8): `onError` fallback sang placeholder — **đã chứng minh hiệu quả trên production**: lúc 502 người dùng thấy gradient thay vì ảnh vỡ. Sau khi Vercel cache ảnh thì hết lỗi (đo được: 35/35 ảnh OK ở lần quét cuối). Chấp nhận, không sửa thêm | AI (đã xử lý), PM (biết để không hoảng nếu thấy vài ảnh thành gradient lúc mới deploy) |
| R6 | Chưa có MapTiler key thật, bản đồ dùng style demo dễ bị rate-limit | Trung bình | Trung bình | PM đăng ký key sớm (song song, không chặn code); AI dùng style demo làm fallback có sẵn | PM |
| R7 | **[Rủi ro AI-sinh]** Rò rỉ secret (API key) vào code/git commit | Thấp | Cao nếu xảy ra | `.env.local` đã trong `.gitignore`; đã kiểm tra không commit key thật nào (hiện tại rỗng); AI không tự điền giá trị secret thay PM | AI + PM (review trước khi push nếu có remote sau này) |
| R8 | Wishlist (phase-2) mất dữ liệu khi đổi thiết bị/xoá cache (localStorage) | Trung bình (chỉ phát sinh khi build tính năng này) | Trung bình | **Chưa chốt mitigation cụ thể** — khi tới lúc code US-09, cân nhắc export/import JSON thủ công (nhẹ, không cần backend) thay vì chấp nhận mất trắng; PM quyết định lúc đó, không quyết vội bây giờ vì ngoài MVP 2 tuần | PM (quyết định khi bắt tay US-09) |
| R9 | Nhạy cảm ranh giới hành chính trước/sau sáp nhập 2025 | Thấp | Thấp (dự án học thuật) | Ghi rõ context "bản đồ dùng ranh giới trước sáp nhập 2025" là theo yêu cầu đề bài (đủ độ chi tiết địa lý cho nội dung ẩm thực vùng miền), không phải quan điểm chính trị | PM |
| **R14** | **[Mới, 2026-09-08] Bản đồ không thể hiện đúng chủ quyền biển đảo Việt Nam** — basemap quốc tế chỉ ghi "Paracel Islands"/"South China Sea"; khung nhìn mặc định lại không bao Hoàng Sa và Trường Sa | **Cao (đã xảy ra thật)** | **Cao** — sai sót về chủ quyền quốc gia, ảnh hưởng nghiêm trọng hơn mọi lỗi kỹ thuật trong dự án | 🟢 **ĐÃ XỬ LÝ 2026-09-08:** mở rộng khung mặc định tới 114,4°Đ/8,0°B bao cả hai quần đảo; thêm lớp `data/sovereignty.json` + `SovereigntyMarker.tsx` hiện ở **mọi mức zoom** với nhãn tiếng Việt kèm đơn vị hành chính (Huyện Hoàng Sa – TP Đà Nẵng; Huyện Trường Sa – tỉnh Khánh Hòa) và nhãn "Biển Đông". Thêm **§US-01c** vào `SIT-UAT-LF.md` để lần sau có cổng kiểm. **Lưu ý quan trọng:** việc đưa centroid Khánh Hòa/Đà Nẵng về đất liền (R13) là quyết định hiển thị marker *ẩm thực*, **không** liên quan chủ quyền — đã ghi rõ trong `DEVBOOK.md` và `scripts/check-geo.mjs` để không bị hiểu sai | PM (nêu yêu cầu, kiểm lại), AI (đã sửa) |
| **R15** | **[Mới, 2026-09-09] Chất lượng bản dịch tiếng Anh chưa ai đọc** — 45.192 từ tiếng Anh do **10 agent sinh song song**, chỉ qua cổng kiểm **cấu trúc** (`check:i18n`: đủ món, đủ trường, khớp số phần tử). **Không cổng nào kiểm NGHĨA.** PM không đọc, AI chỉ đọc kỹ bản thử Hà Nội + rà lướt vài tỉnh | **Cao** — chính các agent tự báo hàng chục "quyết định khi glossary chưa nói rõ"; và bản thử Hà Nội đã lộ 3 bẫy dịch sai (`chao` là động từ, `bánh tráng` trong bánh cuốn, tên loài cá), sau đó còn phát hiện thêm 4 bẫy nữa (`cao lâu` = tiệm ăn người Hoa, `mắm` = cây ngập mặn, `nước mắm` = nước cốt mắm đã lọc, `tráng` là động từ). Số bẫy tìm được tỉ lệ thuận với công sức bỏ ra đọc ⇒ **gần như chắc chắn còn bẫy chưa ai thấy** | Trung bình–Cao. Người đọc tiếng Anh **không có cách nào tự phát hiện** dịch sai — họ không đọc được bản gốc. Khác hẳn R2, nơi người Việt đọc còn nghi ngờ được | 🟡 **Còn mở, ghi thẳng.** Đã có: (a) `I18N-GLOSSARY-LF.md` với mục 4b/4c ghi 7 bẫy đã gặp — dùng lại được cho lần dịch sau; (b) cổng `pnpm check:i18n` chặn lệch cấu trúc; (c) fallback theo từng trường nên thiếu dịch thì hiện tiếng Việt chứ không trống; (d) kênh **Báo nội dung sai** (US-15) hoạt động ở cả bản `/en`. **Chưa có:** ai đó đọc thật 62 tỉnh còn lại. **Đề xuất:** spot-check bản dịch theo đúng cách đã làm với nội dung tiếng Việt — bốc mẫu ngẫu nhiên có seed cố định từ nhóm rủi ro cao (tỉnh nhiều thuật ngữ địa phương: Tây Bắc, Tây Nguyên, Tây Nam Bộ), ghi lại đúng mức đã kiểm | PM (quyết định có spot-check không), AI (đã dựng glossary + cổng cấu trúc) |
| **R16** | **[Mới, 2026-09-11, US-18] Phân loại `mealTypes` sai nằm im** — 197 món do AI gán, **PM chỉ duyệt mẫu**. Món bị gán nhầm thành "mồi nhậu" sẽ **biến mất hoàn toàn** khỏi gợi ý mà không ai biết | Trung bình–Cao. Cùng họ với R2 và R15: phán đoán ngữ nghĩa hàng loạt, chỉ kiểm được mẫu | Trung bình. **Kiểu lỗi nguy hiểm hơn hiển thị sai:** món bị loại nhầm **không xuất hiện để mà bị phát hiện**. Người dùng không thể báo lỗi về một thứ họ không nhìn thấy — khác hẳn R2, nơi nội dung sai vẫn hiện ra và còn bị soi | 🟡 **Còn mở.** (a) Mỗi món ghi **lý do phân loại ngay trong dữ liệu** để soi lại được, không phải hộp đen; (b) cổng `pnpm check:meal` thống kê phân bố và **cảnh báo bất thường** (vd một tỉnh không còn món bữa chính nào — dấu hiệu gán nhầm hàng loạt); (c) W4-9 PM duyệt **nhóm ranh giới** chứ không bốc ngẫu nhiên, vì lỗi tập trung ở đó; (d) trang `/goi-y` ghi rõ phân loại là **phán đoán của người biên tập**. **Chưa có:** cách để người dùng báo *"sao món X không có trong gợi ý"* | PM (duyệt mẫu W4-9), AI (gán + dựng cổng + ghi lý do) |
| **R17** | **[Mới, 2026-09-11, US-18] Tính năng KHẲNG ĐỊNH một điều không nguồn nào kiểm chứng** — "món này hợp dân văn phòng". Khác mọi nội dung khác trên site vốn có `sourceRef` đối chiếu được, đây là **ý kiến** | Cao (đúng bản chất tính năng, không tránh được) | Thấp–Trung bình. Không gây hại thân thể, nhưng **làm mòn nguyên tắc nền của dự án**: mọi khẳng định phải có nguồn. Nếu để trôi, lần sau sẽ dễ dàng thêm nội dung không nguồn khác | 🟡 **Chấp nhận công khai, có rào.** (a) Giao diện **nói thẳng** đây là gợi ý, không phải chuẩn mực, và **không gợi ý quán** (không có giá/địa chỉ); (b) **Cấm tuyệt đối nội dung dinh dưỡng/sức khoẻ** (calo, đường huyết, "tốt cho tim mạch") — đây là ranh giới cứng: dự án không có nguồn y tế nào, nói sai gây hại thật; (c) tiêu chí lọc là **khách quan và kiểm được** (có `mắm tôm` trong nguyên liệu hay không; có gắn `cay` hay không) chứ không phải phán xét mơ hồ kiểu "món này healthy" | PM (chấp nhận rủi ro), AI (giữ đúng ranh giới, không lấn sang sức khoẻ) |
| R10 | **[Mới, sau D3]** Spam/troll/nội dung phản cảm trên review công khai — không có auth, không kiểm duyệt trước khi đăng | Cao (form công khai ẩn danh luôn bị spam bot/troll sớm muộn) | Trung bình (uy tín trang, trải nghiệm người đọc) | 🟡 **Còn mở, đã chấp nhận công khai.** Rào chắn đã code (2026-09-06): giới hạn độ dài ở **cả client lẫn DB** (`check` constraint + RLS `with check`), chặn gửi lặp trong 30s cho cùng món, `maxLength` trên input. PM tự ẩn (`status='hidden'`) qua Supabase dashboard khi phát hiện. **Vẫn không có kiểm duyệt trước khi đăng** — ghi rõ là hạn chế đã biết, không giấu ở viva | PM (theo dõi thủ công), AI (rào chắn kỹ thuật tối thiểu) |
| R11 | **[Mới, sau D3]** Thiếu RLS (Row Level Security) trên bảng Supabase → anon key lộ trong code client cho phép đọc/ghi/xoá toàn bộ dữ liệu | Thấp nếu làm đúng quy trình, **Cao nếu quên** | Cao (mất/hỏng toàn bộ dữ liệu review) | 🟢 **ĐÃ ĐÓNG 2026-09-06 — đóng bằng bằng chứng, không bằng lời hứa.** Không chỉ chạy migration rồi tin là xong, mà **tự tấn công database thật** để kiểm: insert hợp lệ → `201`; ép `status=hidden` → `401`; `rating=99` → `401`; comment 600 ký tự → `401`; `DELETE` cả bảng → `401`; `UPDATE` review người khác → `401`. Thêm giới hạn quyền theo **cột** (`grant insert (…)`) nên client không ghi được `id`/`created_at`/`status`. Câu lệnh tự kiểm nằm ở cuối `supabase/schema.sql` + `SIT-UAT-LF.md` §US-14 mục 0 | AI (code policy + kiểm chứng), PM (chạy lại 2 câu kiểm ở SIT-UAT mục 0 trước khi nộp) |
| R13 | **[Mới, bước [10] 2026-09-06]** Checklist QA (`SIT-UAT-LF.md`) không có mục kiểm **tính đúng đắn địa lý** của marker — mắt người lướt qua bản đồ không tự nhận ra marker đặt sai chỗ | Cao (đã xảy ra thật) | Trung bình (sai lệch nội dung địa lý, ảnh hưởng uy tín) | Đã xảy ra: centroid Khánh Hòa/Đà Nẵng nằm giữa Biển Đông suốt từ walking skeleton, **đi qua trọn vòng QA W1-11 được đánh PASS** mà không ai phát hiện, chỉ lộ khi mở rộng đủ 63 tỉnh. Đã sửa 2 tỉnh + ghi DEVBOOK. 🟢 **ĐÃ ĐÓNG 2026-09-06 bằng 2 lớp:** (1) thêm **§US-01b** vào `SIT-UAT-LF.md` — bước rà mắt vùng Biển Đông, kiểm riêng các tỉnh có đảo; (2) thêm **cổng tự động `pnpm check:geo`** kiểm mọi centroid nằm trong bbox đất liền VN. **Đã chứng minh cổng hoạt động:** đặt lại toạ độ Khánh Hòa cũ → script fail exit code 1, chỉ đúng tên tỉnh. Giờ loại lỗi này bị chặn bằng máy, không phụ thuộc trí nhớ người | AI (đã sửa dữ liệu + dựng cổng), PM (chạy §US-01b trước viva) |
| R12 | **[Mới, W1-11b]** Trang chủ không đạt NFR "LCP < 2.5s" do MapLibre GL JS (thư viện bản đồ WebGL) tự thân nặng ~700KB-1MB script, chiếm 6-10s scripting lúc khởi tạo | Cao (đã đo được, không phải suy đoán) | Trung bình (UX vẫn dùng được, chỉ điểm Lighthouse xấu) | Chấp nhận đánh đổi — bản đồ là tính năng lõi bắt buộc theo đề bài, không thể bỏ để đạt điểm đẹp hơn. Đã tối ưu phần trong tầm kiểm soát (ảnh marker 14MB→211KB). **Đo lại 2026-09-07 với đủ 63 tỉnh (trung vị 3 lần): LCP 6,20s — TỐT HƠN mức 8,8s hồi chỉ có 8 tỉnh**, payload giảm 19% dù dữ liệu gấp 8 lần, nhờ marker nhỏ theo zoom + `fitBounds` + 31 tỉnh dùng gradient không tốn request. Vẫn không đạt ngưỡng 2,5s, nhưng khoảng cách thu hẹp từ ~6,3s xuống ~3,7s và phần vượt là chi phí cố hữu của MapLibre. Trang không có bản đồ đạt 80/100. Ghi đủ trong `PERFORMANCE-LF.md`, không che giấu ở hồ sơ viva | PM (chấp nhận rủi ro), AI (đã tối ưu phần khả thi) |

---

## 🔒 Cổng hiểu — bước [6] (chung với `DELEGATION-MAP-LF.md`)

Xem cổng hiểu ở cuối `DELEGATION-MAP-LF.md` — 2 artefact này dùng chung 1
cổng vì cùng thuộc bước [6] theo Capstone Playbook.
